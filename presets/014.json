{
  "name": "014",
  "description": "Colored Flock - Pastel huePhase, Small Scale",
  "tags": [],
  "code": "// Colored Flock - Pastel huePhase, Small Scale\n\n// ---- audio (smoothed envelopes) ----\nAUDIO.bins = 4; AUDIO.smooth = 0.85; AUDIO.apply();\nvar _lo = function(){ return a.fft[0] || 0 };\nvar _mi = function(){ return a.fft[2] || 0 };\nvar low = env(_lo, 0.25, 0.06);\nvar mid = env(_mi, 0.3, 0.08);\nvar amp = function(){ return (low() + mid()) * 0.5 };\n\n// ---- flock params ----\nconst DEPTH = 3;\nconst COLS = 10;\nconst EDGE = 0.009;\nconst ROUND = 0.02;\nconst ROT_S = 0.1;\nconst FLOW = 0.1;\n\nconst bird = () => shape(200, EDGE, ROUND);\n\n// small helper: soft pastel RGB generator (based on index/time/depth)\nfunction birdColor(i, d) {\n    const huePhase = i * 0.16 + time * 0.05 + d * 0.6;\n    const r = 0.95 + 0.15 * Math.sin(huePhase + 0.0) * (1 - d * 0.7) + amp() * 0.001;\n    const g = 0.85 + 0.14 * Math.sin(huePhase + 2.0) * (1 - d * 0.6) + low() * 0.001;\n    const b = 0.85 + 0.13 * Math.sin(huePhase + 4.0) * (1 - d * 0.5) + mid() * 0.001;\n    return [r, g, b];\n}\n\n// flow fields (tiny, slow)\nconst flowX = (i, d) => Math.sin(time * 0.12 + i * 0.6) * FLOW * (1 - d) * (1 + low() * 0.2);\nconst flowY = (i, d) => Math.cos(time * 0.10 + i * 0.7) * (FLOW * 0.8) * (1 - d) * (1 + mid() * 0.2);\n\n// build a layer of birds (returns a Hydra src)\nfunction flockLayer(i) {\n    const d = i / (DEPTH - 1);\n    const cols = Math.max(4, Math.floor(COLS - i * 2));\n    let layer = bird()\n        .repeat(cols, 8)\n        .scale(() => 0.5 - d * 0.15 + Math.sin(time * 0.5 + i) * 0.02 + amp() * 0.02)\n        .rotate(() => time * ROT_S * (1 + d * 0.15) + Math.sin(time * 0.07 + i) * 0.02)\n        .scrollX(() => flowX(i, d))\n        .scrollY(() => flowY(i, d))\n        .modulate(noise(0.9 - d * 0.3, 0.02 + d * 0.05), 0.08 + d * 0.12)\n        .brightness(0.018 + d * 0.005);\n\n    const colTint = () => {\n        const idx = i;\n        const c = birdColor(idx, d);\n        const gain = 0.08 + amp() * 0.01;\n        return [c[0] + gain * low(), c[1] + gain * mid(), c[2] + gain * 0.5];\n    };\n\n    return layer.color(() => {\n        const cc = colTint();\n        return cc[0];\n    }, () => {\n        const cc = colTint();\n        return cc[1];\n    }, () => {\n        const cc = colTint();\n        return cc[2];\n    });\n}\n\n// render layers to internal buffers\nflockLayer(0).out(o1);\nflockLayer(1).out(o2);\nflockLayer(2).out(o3);\n\n// composite: keep contrast gentle so the colors read as pastel\nconst baseComp = () => src(o1)\n    .add(src(o2), 0.25)\n    .add(src(o3), 0.3)\n    .contrast(1.01)\n    .saturate(1.02);\n\n// draw once into o0\nbaseComp().out(o0);\n\n// ---- chromatic aberration rainbow tracer ----\nconst aberr = () => src(o0)\n    .r()\n    .scrollX(0.002 + low() * 0.0012)\n    .scrollY(0.0012 + mid() * 0.0008)\n    .color(1, 0.2, 0.2)\n    .add(\n        src(o0)\n        .g()\n        .scrollX(-(0.0016 + low() * 0.0008))\n        .scrollY(-(0.0011 + mid() * 0.0007))\n        .color(0.4, 1, 0.4)\n    )\n    .add(\n        src(o0)\n        .b()\n        .scrollY(0.0017 + amp() * 0.0009)\n        .color(0.2, 0.3, 1)\n    )\n    .saturate(1.09)\n    .colorama(0.002 + amp() * 0.004);\n\n// ---- gentle feedback trail ----\nconst trail = () => src(o0)\n    .blend(baseComp(), 0.51 + amp() * 0.15)\n    .modulateScale(noise(1.6, 0.03), 0.003 + amp() * 0.0008)\n    .modulateRotate(noise(0.2, 0.02), 0.002)\n    .luma(0.005, 0.07)\n    .brightness(0.027);\n\n// final composite: colored flock + rainbow edges + restrained trail\naberr()\n    .blend(trail(), 0.688)\n    .contrast(1.032)\n    .saturate(1.59)\n    .out();",
  "filters": {
    "hue": 329,
    "sat": 287,
    "bright": 163,
    "contrast": 300,
    "speed": 1
  }
}
