{
  "name": "025",
  "description": "Video feedback — feedback_abstract with gradient tint",
  "tags": [],
  "code": "AUDIO.bins = 4; AUDIO.smooth = 0.88; AUDIO.apply();\nvar low = env(()=> a.fft[0]||0, 0.25, 0.06);\nvar mid = env(()=> a.fft[1]||0, 0.3, 0.08);\nvar high = env(()=> a.fft[3]||0, 0.2, 0.1);\n\ns0.initVideo('/dist/media/feedback_abstract.mp4')\n\n// feedback layer — seeded from video, builds abstract texture\nsrc(s0).luma(0.3, 0.1)\n  .blend(src(o1).scale(1.002).rotate(0.003).saturate(1.3), 0.78)\n  .modulate(noise(2, 0.6), ()=>0.015+mid()*0.006)\n  .out(o1)\n\n// video on top with subtle color tint\nsrc(s0)\n  .add(gradient(0.3).hue(()=>time*0.02).saturate(0.6), 0.12)\n  .layer(src(o1).mult(solid(1,1,1), 0.2))\n  .modulate(src(o1), ()=>0.006+mid()*0.003)\n  .out(o0)\n\nrender(o0)",
  "filters": {
    "hue": 120,
    "sat": 300,
    "bright": 148,
    "contrast": 81,
    "speed": 0.5
  }
}
