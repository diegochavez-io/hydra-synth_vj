# 003

```js
// ---- audio (smoothed envelopes) ----
AUDIO.bins = 6; AUDIO.smooth = 0.88; AUDIO.apply();
var _lo = function(){ return a.fft[0] || 0 };
var _mi = function(){ return a.fft[2] || 0 };
var _hi = function(){ return a.fft[4] || 0 };
var low = env(_lo, 0.25, 0.06);
var mid = env(_mi, 0.3, 0.08);
var high = env(_hi, 0.2, 0.1);
var amp = function(){ return (low() + mid() + high()) * 0.9 };

osc (30, 0.2, 2)
.color (()=>1.4 + low()*0.15, ()=>0.2 + mid()*0.1, ()=>-0.1 + high()*0.12)
.modulate(osc(2))
.modulateScale(noise(-2, 0.04))
.modulateRepeat( o0, repeatX = 1, repeatY = 1, offsetX = 0.1, offsetY = 0.1 )
.blend (o0, 0.50)
.modulateRepeat( o0, repeatX = 1, repeatY = 1, offsetX = 0.1, offsetY = 0.1 )
.saturate(()=> 1.0 + low()*0.12)
.hue(()=> low()*0.01)
.colorama(()=> 0.002 + amp()*0.004)
.out (o0)
```

<!-- filters:{"hue":0,"sat":235,"bright":86,"contrast":100,"speed":1} -->
