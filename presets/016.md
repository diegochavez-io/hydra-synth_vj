# 016

```js
// ---- audio (smoothed envelopes) ----
AUDIO.bins = 4; AUDIO.smooth = 0.85; AUDIO.apply();
var low = env(()=> a.fft[0]||0, 0.3, 0.05);
var mid = env(()=> a.fft[1]||0, 0.25, 0.06);
var high = env(()=> a.fft[3]||0, 0.2, 0.08);

speed = 0.2
osc (20, 0.2,2)
.modulate(noise(8, 0.01), 0.02)
.color (()=>1.1 + low()*0.12, ()=>0.5 + mid()*0.1, [0.5,0.8,-1.5, 1.8])
.modulate(osc(9))
.kaleid([2,4,6,-4,-2])
.blend (o0, 0.90)
.modulateScale(noise(-2,0.04))
.saturate(()=> 1.0 + low()*0.12)
.hue(()=> mid()*0.006)
.colorama(()=> 0.001 + high()*0.002)
.out (o0)
```

<!-- filters:{"hue":0,"sat":100,"bright":100,"contrast":100,"speed":1} -->
